# AI 应用中心 - 页面设计实施方案

> 基于设计稿制定的完整技术实施计划
>
> **当前版本**: v1.0
> **制定日期**: 2026-02-13
> **设计风格**: 赛博朋克（Cyberpunk）+ 现代极简

---

## 📋 目录

1. [设计系统规范](#设计系统规范)
2. [首页实施方案](#首页实施方案)
3. [列表页实施方案](#列表页实施方案)
4. [表单页实施方案](#表单页实施方案)
5. [通用组件库](#通用组件库)
6. [实施路线图](#实施路线图)

---

## 🎨 设计系统规范

### 1.1 色彩体系（已完成）

**主色调 - 赛博朋克风格**：
```css
/* 浅色模式 */
--primary: oklch(0.65 0.25 310);    /* 霓虹紫 */
--accent: oklch(0.75 0.22 180);     /* 青色强调 */
--background: oklch(0.97 0.01 280); /* 浅灰紫背景 */

/* 深色模式 */
--primary: oklch(0.70 0.28 310);    /* 亮紫霓虹 */
--accent: oklch(0.80 0.25 180);     /* 霓虹青色 */
--background: oklch(0.08 0.03 280); /* 深紫背景 */
```

**语义化颜色**：
- 成功/增长：`green-500` / `green-600`
- 警告/进行中：`yellow-500` / `amber-500`
- 危险/下降：`red-500` / `destructive`
- 信息：`cyan-500` / `accent`

### 1.2 字体体系（已完成）

- **标题字体**: Poppins (现代、清晰、科技感)
- **代码/数据字体**: JetBrains Mono (等宽专业)
- **字号比例**:
  - 页面标题: `text-4xl` (36px)
  - 卡片标题: `text-2xl` (24px)
  - 正文: `text-base` (16px)
  - 小字: `text-sm` (14px)
  - 标签: `text-xs` (12px)

### 1.3 间距体系

- **卡片外边距**: `gap-4` (16px)
- **内容区域**: `p-6` ~ `p-8` (24-32px)
- **组件间距**: `gap-2` ~ `gap-6` (8-24px)

### 1.4 圆角规范

- **卡片**: `rounded-2xl` (1rem)
- **按钮**: `rounded-xl` (0.75rem)
- **输入框**: `rounded-lg` (0.5rem)
- **标签**: `rounded-md` (0.375rem)

---

## 🏠 首页实施方案

### 设计稿分析

**设计稿文件**: `docs/页面设计/首页/screen.png`

**核心组件**：
1. ✅ 统计卡片区域（已实现）
2. 📊 趋势图表
3. 🚀 快捷应用网格
4. 📝 待办事项列表
5. 📅 日历组件

### 2.1 当前状态

**已完成部分**：
- ✅ 顶部导航栏 (SiteHeader)
- ✅ 左侧边栏 (AppSidebar)
- ✅ 统计卡片 (StatCards) - 已应用赛博朋克风格
- ✅ 访客图表 (VisitorsChart)
- ✅ 数据表格 (DashboardTable)

**文件位置**：
- 主页面: `app/(pages)/dashboard/page.tsx`
- 统计卡片: `app/(pages)/dashboard/components/stat-cards.tsx`

### 2.2 需要新增的组件

#### A. 快捷应用网格

**位置**: `app/(pages)/dashboard/components/quick-apps.tsx`

**设计要求**：
```tsx
// 6 个应用图标网格布局
// 每个应用卡片：图标 + 名称 + 状态指示器
// Hover 效果：缩放 + 阴影增强
```

**实现方案**：
```tsx
<div className="grid grid-cols-2 md:grid-cols-3 gap-4">
  {apps.map((app) => (
    <Card className="group hover:scale-105 transition-all cursor-pointer">
      <CardContent className="flex flex-col items-center p-6 gap-3">
        {/* 图标 - 带渐变背景 */}
        <div className="relative">
          <div className="absolute inset-0 bg-primary/20 blur-xl" />
          <div className="relative p-4 rounded-2xl bg-gradient-to-br from-primary to-accent">
            <app.Icon className="h-8 w-8 text-white" />
          </div>
        </div>

        {/* 应用名称 */}
        <h3 className="font-bold text-sm">{app.name}</h3>

        {/* 状态指示 */}
        <Badge variant="outline">{app.status}</Badge>
      </CardContent>
    </Card>
  ))}
</div>
```

**数据源**：
- 从数据库读取用户常用应用
- Server Action: `app/actions/dashboard.ts`

#### B. 待办事项组件

**位置**: `app/(pages)/dashboard/components/todo-list.tsx`

**功能需求**：
- 显示待办任务列表（标题、优先级、截止日期）
- 任务状态切换（复选框）
- 优先级颜色标识
- 支持快速添加

**实现方案**：
```tsx
interface TodoItem {
  id: string;
  title: string;
  priority: 'high' | 'medium' | 'low';
  dueDate?: string;
  completed: boolean;
}

<Card>
  <CardHeader>
    <CardTitle>待办事项</CardTitle>
    <Button size="sm">添加任务</Button>
  </CardHeader>
  <CardContent>
    <div className="space-y-3">
      {todos.map((todo) => (
        <div className="flex items-center gap-3 p-3 rounded-lg hover:bg-muted/50">
          <Checkbox checked={todo.completed} />
          <div className="flex-1">
            <p className={cn("font-medium", todo.completed && "line-through")}>
              {todo.title}
            </p>
            {todo.dueDate && (
              <p className="text-xs text-muted-foreground">
                截止: {formatDate(todo.dueDate)}
              </p>
            )}
          </div>
          <Badge variant={priorityVariant[todo.priority]}>
            {priorityLabel[todo.priority]}
          </Badge>
        </div>
      ))}
    </div>
  </CardContent>
</Card>
```

#### C. 日历视图组件

**位置**: `app/(pages)/dashboard/components/calendar-widget.tsx`

**依赖**:
- 使用现有的 `react-day-picker` (已安装)
- UI 组件: `components/ui/calendar.tsx` (已存在)

**实现方案**：
```tsx
<Card>
  <CardHeader>
    <CardTitle>日历</CardTitle>
  </CardHeader>
  <CardContent>
    <Calendar
      mode="single"
      selected={selectedDate}
      onSelect={setSelectedDate}
      className="rounded-md border"
      modifiers={{
        hasEvent: datesWithEvents,
      }}
      modifiersStyles={{
        hasEvent: {
          backgroundColor: 'var(--primary)',
          color: 'white',
          fontWeight: 'bold',
        }
      }}
    />

    {/* 选中日期的事件列表 */}
    {selectedDateEvents.length > 0 && (
      <div className="mt-4 space-y-2">
        {selectedDateEvents.map((event) => (
          <div className="p-2 rounded-lg bg-primary/10">
            <p className="text-sm font-medium">{event.title}</p>
            <p className="text-xs text-muted-foreground">{event.time}</p>
          </div>
        ))}
      </div>
    )}
  </CardContent>
</Card>
```

### 2.3 布局优化

**当前布局**：垂直堆叠（StatCards → Chart → Table）

**目标布局**：网格化（参考设计稿）

```tsx
// app/(pages)/dashboard/page.tsx
<div className="grid grid-cols-1 lg:grid-cols-12 gap-6">
  {/* 统计卡片 - 全宽 */}
  <div className="lg:col-span-12">
    <StatCards />
  </div>

  {/* 趋势图表 - 左侧 8 列 */}
  <div className="lg:col-span-8">
    <VisitorsChart />
  </div>

  {/* 快捷应用 - 右侧 4 列 */}
  <div className="lg:col-span-4">
    <QuickApps />
  </div>

  {/* 待办事项 - 左侧 4 列 */}
  <div className="lg:col-span-4">
    <TodoList />
  </div>

  {/* 日历 - 中间 4 列 */}
  <div className="lg:col-span-4">
    <CalendarWidget />
  </div>

  {/* 最近活动 - 右侧 4 列 */}
  <div className="lg:col-span-4">
    <RecentActivity />
  </div>
</div>
```

---

## 📊 列表页实施方案

### 设计稿分析

**设计稿文件**: `docs/页面设计/列表/screen.png`

**核心要素**：
1. 🔍 搜索筛选区
2. 📋 数据表格
3. 🏷️ 状态标签
4. ⚙️ 操作菜单
5. 📄 分页控件

### 3.1 通用列表页架构

**适用场景**：
- 订单管理
- 用户管理 (`/users`)
- 应用管理 (`/apps`)
- 项目管理

**目录结构**：
```
app/(pages)/[module]/
├── page.tsx              # 服务端页面
├── components/
│   ├── data-table.tsx    # 数据表格
│   ├── columns.tsx       # 列定义
│   ├── filters.tsx       # 筛选器
│   └── actions-menu.tsx  # 操作菜单
└── [id]/
    └── page.tsx          # 详情页（可选）
```

### 3.2 搜索筛选区设计

**位置**: `components/common/list-filters.tsx`

```tsx
interface FilterConfig {
  searchPlaceholder: string;
  dateRangeEnabled?: boolean;
  statusOptions?: Array<{ value: string; label: string }>;
  customFilters?: React.ReactNode;
}

<Card className="mb-6">
  <CardContent className="p-4">
    <div className="grid grid-cols-1 md:grid-cols-12 gap-4">
      {/* 搜索框 - 占 4 列 */}
      <div className="md:col-span-4">
        <div className="relative">
          <Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />
          <Input
            placeholder={config.searchPlaceholder}
            className="pl-9"
            value={searchTerm}
            onChange={(e) => setSearchTerm(e.target.value)}
          />
        </div>
      </div>

      {/* 状态筛选 - 占 3 列 */}
      {config.statusOptions && (
        <div className="md:col-span-3">
          <Select value={status} onValueChange={setStatus}>
            <SelectTrigger>
              <SelectValue placeholder="全部状态" />
            </SelectTrigger>
            <SelectContent>
              {config.statusOptions.map((opt) => (
                <SelectItem key={opt.value} value={opt.value}>
                  {opt.label}
                </SelectItem>
              ))}
            </SelectContent>
          </Select>
        </div>
      )}

      {/* 日期范围 - 占 5 列 */}
      {config.dateRangeEnabled && (
        <div className="md:col-span-5 flex gap-2">
          <DatePicker
            date={startDate}
            onDateChange={setStartDate}
            placeholder="开始日期"
          />
          <span className="flex items-center text-muted-foreground">至</span>
          <DatePicker
            date={endDate}
            onDateChange={setEndDate}
            placeholder="结束日期"
          />
        </div>
      )}

      {/* 操作按钮区 */}
      <div className="md:col-span-12 flex gap-2 justify-between">
        <div className="flex gap-2">
          <Badge variant="outline" className="cursor-pointer">
            <input type="checkbox" className="mr-2" />
            进行中
          </Badge>
          <Badge variant="outline" className="cursor-pointer">
            <input type="checkbox" className="mr-2" />
            已完成
          </Badge>
        </div>

        <div className="flex gap-2">
          <Button variant="outline" size="sm">
            <Filter className="h-4 w-4 mr-2" />
            查询
          </Button>
          <Button variant="ghost" size="sm">
            重置
          </Button>
        </div>
      </div>
    </div>
  </CardContent>
</Card>
```

### 3.3 数据表格优化

**当前实现**: `components/common/data-table.tsx` (已存在)

**需要增强的功能**：
1. 批量选择
2. 批量操作
3. 排序功能
4. 列可见性控制

**增强方案**：
```tsx
// 添加批量操作工具栏
{table.getFilteredSelectedRowModel().rows.length > 0 && (
  <div className="flex items-center gap-2 p-4 bg-primary/10 rounded-lg mb-4">
    <p className="text-sm font-medium">
      已选择 {table.getFilteredSelectedRowModel().rows.length} 项
    </p>
    <div className="flex gap-2 ml-auto">
      <Button variant="outline" size="sm">
        <Download className="h-4 w-4 mr-2" />
        导出
      </Button>
      <Button variant="outline" size="sm">
        <Printer className="h-4 w-4 mr-2" />
        打印
      </Button>
      <Button variant="destructive" size="sm">
        <Trash className="h-4 w-4 mr-2" />
        批量删除
      </Button>
    </div>
  </div>
)}
```

### 3.4 状态徽章样式

**统一的状态颜色规范**：
```tsx
const statusConfig = {
  // 进行中/处理中
  pending: {
    variant: 'outline' as const,
    className: 'bg-yellow-500/10 text-yellow-600 border-yellow-500/20',
    label: '进行中'
  },
  // 已完成/已通过
  completed: {
    variant: 'outline' as const,
    className: 'bg-green-500/10 text-green-600 border-green-500/20',
    label: '已完成'
  },
  // 已取消/已拒绝
  cancelled: {
    variant: 'outline' as const,
    className: 'bg-gray-500/10 text-gray-600 border-gray-500/20',
    label: '已取消'
  },
  // 草稿
  draft: {
    variant: 'outline' as const,
    className: 'bg-blue-500/10 text-blue-600 border-blue-500/20',
    label: '草稿'
  },
};

// 使用示例
<Badge
  variant={statusConfig[status].variant}
  className={statusConfig[status].className}
>
  {statusConfig[status].label}
</Badge>
```

---

## 📝 表单页实施方案

### 设计稿分析

**设计稿文件**: `docs/页面设计/表单/screen.png`

**核心特点**：
1. 分区块表单布局
2. 清晰的视觉层次
3. 内联验证反馈
4. 文件上传组件
5. 底部固定操作栏

### 4.1 表单布局组件

**位置**: `components/common/form-layout.tsx`

```tsx
interface FormSection {
  title: string;
  description?: string;
  fields: React.ReactNode;
}

interface FormLayoutProps {
  title: string;
  subtitle?: string;
  sections: FormSection[];
  onSubmit: () => void;
  onCancel: () => void;
  isLoading?: boolean;
}

export function FormLayout({
  title,
  subtitle,
  sections,
  onSubmit,
  onCancel,
  isLoading
}: FormLayoutProps) {
  return (
    <div className="min-h-screen bg-background">
      {/* 顶部标题区 */}
      <div className="border-b bg-card/50 backdrop-blur-sm sticky top-0 z-10">
        <div className="max-w-4xl mx-auto px-6 py-6">
          <h1 className="text-3xl font-bold">{title}</h1>
          {subtitle && (
            <p className="text-muted-foreground mt-2">{subtitle}</p>
          )}
        </div>
      </div>

      {/* 表单主体 */}
      <div className="max-w-4xl mx-auto px-6 py-8">
        <form onSubmit={onSubmit} className="space-y-8">
          {sections.map((section, index) => (
            <Card key={index}>
              <CardHeader>
                <CardTitle className="text-lg">{section.title}</CardTitle>
                {section.description && (
                  <CardDescription>{section.description}</CardDescription>
                )}
              </CardHeader>
              <CardContent className="space-y-4">
                {section.fields}
              </CardContent>
            </Card>
          ))}
        </form>
      </div>

      {/* 底部固定操作栏 */}
      <div className="fixed bottom-0 left-0 right-0 border-t bg-card/80 backdrop-blur-sm z-20">
        <div className="max-w-4xl mx-auto px-6 py-4 flex justify-between items-center">
          <Button
            variant="ghost"
            onClick={onCancel}
            disabled={isLoading}
          >
            放弃编辑
          </Button>

          <div className="flex gap-3">
            <Button
              variant="outline"
              onClick={() => onSubmit('draft')}
              disabled={isLoading}
            >
              保存草稿
            </Button>
            <Button
              type="submit"
              onClick={() => onSubmit('submit')}
              disabled={isLoading}
              className="bg-gradient-to-r from-primary to-accent"
            >
              {isLoading ? (
                <>
                  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                  提交中...
                </>
              ) : (
                '提交保存'
              )}
            </Button>
          </div>
        </div>
      </div>

      {/* 底部占位，避免内容被固定栏遮挡 */}
      <div className="h-20" />
    </div>
  );
}
```

### 4.2 文件上传组件

**位置**: `components/common/file-upload.tsx`

```tsx
interface FileUploadProps {
  accept?: string;
  maxSize?: number; // MB
  multiple?: boolean;
  onFileSelect: (files: File[]) => void;
}

export function FileUpload({
  accept = ".pdf,.jpg,.png",
  maxSize = 10,
  multiple = false,
  onFileSelect
}: FileUploadProps) {
  const [isDragging, setIsDragging] = useState(false);
  const [files, setFiles] = useState<File[]>([]);

  return (
    <div className="space-y-4">
      {/* 上传区域 */}
      <div
        className={cn(
          "border-2 border-dashed rounded-xl p-8 text-center transition-all",
          isDragging
            ? "border-primary bg-primary/5"
            : "border-border hover:border-primary/50"
        )}
        onDrop={handleDrop}
        onDragOver={handleDragOver}
        onDragLeave={() => setIsDragging(false)}
      >
        <div className="flex flex-col items-center gap-3">
          <div className="p-4 rounded-full bg-primary/10">
            <Upload className="h-8 w-8 text-primary" />
          </div>

          <div>
            <p className="font-medium">点击上传或拖拽文件到此处</p>
            <p className="text-sm text-muted-foreground mt-1">
              支持 {accept} 格式，最大 {maxSize}MB
            </p>
          </div>

          <Button variant="outline" size="sm" onClick={triggerFileInput}>
            选择文件
          </Button>
        </div>

        <input
          ref={fileInputRef}
          type="file"
          accept={accept}
          multiple={multiple}
          className="hidden"
          onChange={handleFileSelect}
        />
      </div>

      {/* 已选文件列表 */}
      {files.length > 0 && (
        <div className="space-y-2">
          <p className="text-sm font-medium">已选择文件：</p>
          {files.map((file, index) => (
            <div
              key={index}
              className="flex items-center justify-between p-3 bg-muted/50 rounded-lg"
            >
              <div className="flex items-center gap-3">
                <FileIcon className="h-5 w-5 text-primary" />
                <div>
                  <p className="text-sm font-medium">{file.name}</p>
                  <p className="text-xs text-muted-foreground">
                    {(file.size / 1024 / 1024).toFixed(2)} MB
                  </p>
                </div>
              </div>

              <Button
                variant="ghost"
                size="sm"
                onClick={() => removeFile(index)}
              >
                <X className="h-4 w-4" />
              </Button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

### 4.3 表单验证增强

**使用 React Hook Form + Zod**（已安装）

```tsx
// 示例：订单表单 Schema
const orderFormSchema = z.object({
  name: z.string().min(2, "订单名称至少2个字符").max(50),
  customerId: z.string().min(1, "请选择客户"),
  expectedDate: z.date({
    required_error: "请选择期望交付日期",
  }),
  priority: z.enum(["normal", "urgent"], {
    required_error: "请选择优先级",
  }),
  description: z.string().optional(),
  attachments: z.array(z.instanceof(File)).optional(),
});

// 表单组件中使用
const form = useForm<z.infer<typeof orderFormSchema>>({
  resolver: zodResolver(orderFormSchema),
  defaultValues: {
    priority: "normal",
  },
});

// 字段渲染
<FormField
  control={form.control}
  name="name"
  render={({ field }) => (
    <FormItem>
      <FormLabel>订单名称 <span className="text-destructive">*</span></FormLabel>
      <FormControl>
        <Input placeholder="请输入订单名称" {...field} />
      </FormControl>
      <FormMessage />
    </FormItem>
  )}
/>
```

---

## 🧩 通用组件库

### 5.1 需要新增的组件

| 组件名 | 位置 | 用途 | 优先级 |
|--------|------|------|--------|
| `FileUpload` | `components/common/file-upload.tsx` | 文件上传 | 🔴 高 |
| `DateRangePicker` | `components/common/date-range-picker.tsx` | 日期范围选择 | 🔴 高 |
| `StatusBadge` | `components/common/status-badge.tsx` | 统一状态标签 | 🟡 中 |
| `QuickApps` | `app/(pages)/dashboard/components/quick-apps.tsx` | 快捷应用网格 | 🔴 高 |
| `TodoList` | `app/(pages)/dashboard/components/todo-list.tsx` | 待办列表 | 🟡 中 |
| `CalendarWidget` | `app/(pages)/dashboard/components/calendar-widget.tsx` | 日历组件 | 🟢 低 |
| `FormLayout` | `components/common/form-layout.tsx` | 通用表单布局 | 🔴 高 |
| `ListFilters` | `components/common/list-filters.tsx` | 列表筛选器 | 🔴 高 |

### 5.2 组件复用策略

**已有组件**（充分利用）：
- ✅ `Button` - 按钮
- ✅ `Card` - 卡片容器
- ✅ `Input` - 输入框
- ✅ `Select` - 下拉选择
- ✅ `Badge` - 标签
- ✅ `Dialog` / `Sheet` - 弹窗/抽屉
- ✅ `DataTable` - 数据表格
- ✅ `Calendar` - 日历（react-day-picker）

**设计一致性要点**：
1. 所有新组件遵循赛博朋克配色
2. 使用统一的圆角规范（`rounded-xl`）
3. Hover 效果统一（`hover:scale-[1.02]` + 阴影）
4. 动画使用 `transition-all duration-300`

---

## 🗺️ 实施路线图

### Phase 1: 基础优化（1-2 天）✅ 已完成

- [x] 字体系统升级
- [x] 配色方案更新
- [x] 登录/注册页面优化
- [x] Dashboard 统计卡片增强
- [x] 首次登录欢迎体验

### Phase 2: 首页完善（2-3 天）

**优先级：🔴 高**

- [ ] **Step 2.1**: 快捷应用组件
  - 创建 `quick-apps.tsx`
  - 实现网格布局 + hover 效果
  - 集成应用数据源

- [ ] **Step 2.2**: 待办事项组件
  - 创建 `todo-list.tsx`
  - 实现任务列表 + 状态切换
  - 添加优先级标识

- [ ] **Step 2.3**: 日历组件
  - 创建 `calendar-widget.tsx`
  - 集成 `react-day-picker`
  - 实现事件标记

- [ ] **Step 2.4**: 首页布局重构
  - 采用网格布局
  - 响应式适配
  - 组件排列优化

### Phase 3: 列表页通用化（2-3 天）

**优先级：🔴 高**

- [ ] **Step 3.1**: 搜索筛选组件
  - 创建 `list-filters.tsx`
  - 实现多条件筛选
  - 日期范围选择器

- [ ] **Step 3.2**: 数据表格增强
  - 添加批量选择
  - 批量操作工具栏
  - 列排序功能

- [ ] **Step 3.3**: 状态标签统一
  - 创建 `status-badge.tsx`
  - 定义状态配色规范
  - 全局替换现有标签

- [ ] **Step 3.4**: 示例页面实现
  - 创建订单管理示例页
  - 演示完整列表功能
  - 编写使用文档

### Phase 4: 表单页标准化（2-3 天）

**优先级：🟡 中**

- [ ] **Step 4.1**: 表单布局组件
  - 创建 `form-layout.tsx`
  - 实现分区块布局
  - 底部固定操作栏

- [ ] **Step 4.2**: 文件上传组件
  - 创建 `file-upload.tsx`
  - 拖拽上传支持
  - 文件预览和删除

- [ ] **Step 4.3**: 表单验证优化
  - Zod schema 模板
  - 内联错误提示样式
  - 实时验证反馈

- [ ] **Step 4.4**: 示例表单实现
  - 创建订单表单示例
  - 演示完整流程
  - 编写使用文档

### Phase 5: 细节打磨（1-2 天）

**优先级：🟢 低**

- [ ] **Step 5.1**: 动画效果统一
  - 页面切换动画
  - 列表项进入动画
  - 加载骨架屏

- [ ] **Step 5.2**: 响应式完善
  - 移动端适配
  - 平板尺寸优化
  - 触摸交互优化

- [ ] **Step 5.3**: 性能优化
  - 懒加载组件
  - 图片优化
  - 缓存策略

- [ ] **Step 5.4**: 无障碍支持
  - ARIA 标签
  - 键盘导航
  - 屏幕阅读器支持

---

## 📁 文件结构建议

```
app/(pages)/
├── dashboard/               # 首页
│   ├── page.tsx
│   └── components/
│       ├── stat-cards.tsx   ✅ 已完成
│       ├── visitors-chart.tsx ✅ 已完成
│       ├── quick-apps.tsx   🚧 待开发
│       ├── todo-list.tsx    🚧 待开发
│       └── calendar-widget.tsx 🚧 待开发
│
├── orders/                  # 示例：订单管理
│   ├── page.tsx             # 列表页
│   ├── new/
│   │   └── page.tsx         # 新增表单
│   └── components/
│       ├── order-filters.tsx
│       ├── order-columns.tsx
│       └── order-form.tsx
│
└── [其他模块]/

components/
├── common/                  # 通用业务组件
│   ├── file-upload.tsx      🚧 待开发
│   ├── date-range-picker.tsx 🚧 待开发
│   ├── status-badge.tsx     🚧 待开发
│   ├── list-filters.tsx     🚧 待开发
│   └── form-layout.tsx      🚧 待开发
│
└── ui/                      # 基础 UI 组件
    ├── button.tsx           ✅
    ├── card.tsx             ✅
    ├── input.tsx            ✅
    └── ...                  ✅
```

---

## 🎯 成功指标

**视觉一致性**：
- [ ] 所有页面遵循赛博朋克配色
- [ ] 组件样式统一（圆角、间距、阴影）
- [ ] 动画效果流畅自然

**功能完整性**：
- [ ] 首页所有组件正常运行
- [ ] 列表页支持搜索、筛选、分页
- [ ] 表单页支持验证、文件上传

**用户体验**：
- [ ] 页面加载速度 < 2s
- [ ] 交互响应 < 100ms
- [ ] 移动端完全可用

**代码质量**：
- [ ] 组件复用率 > 80%
- [ ] TypeScript 类型完整
- [ ] 无 ESLint 错误

---

## 📚 参考资源

**设计稿位置**：
- 首页: `docs/页面设计/首页/screen.png`
- 列表: `docs/页面设计/列表/screen.png`
- 表单: `docs/页面设计/表单/screen.png`

**现有文档**：
- 用户管理: `docs/用户管理/2_页面设计.md`
- 应用管理: `docs/应用管理/2_页面设计.md`
- 开发规范: `docs/开发规范/4_技术栈与组件规范.md`

**技术栈**：
- Next.js 16
- React 19
- Tailwind CSS 4
- Shadcn/UI
- React Hook Form + Zod

---

## 💡 实施建议

1. **优先完成 Phase 2（首页）**
   - 用户打开应用首先看到的页面
   - 展示整体设计风格
   - 包含核心功能入口

2. **然后完成 Phase 3（列表页）**
   - 最常用的页面类型
   - 通用化后可复用
   - 直接提升开发效率

3. **最后完成 Phase 4（表单页）**
   - 使用频率相对较低
   - 可以逐步优化
   - 重点是验证和文件上传

4. **持续优化 Phase 5**
   - 根据用户反馈调整
   - 性能监控和优化
   - 无障碍支持

---

**制定人**: Claude Sonnet 4.5
**审核人**: [待填写]
**批准日期**: [待填写]

---

## 附录：快速开始示例

### 示例 1: 创建快捷应用组件

```bash
# 创建组件文件
touch app/(pages)/dashboard/components/quick-apps.tsx

# 添加数据接口
touch app/actions/apps.ts
```

```tsx
// app/(pages)/dashboard/components/quick-apps.tsx
"use client";

import { Card, CardContent } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { LucideIcon } from "lucide-react";

interface App {
  id: string;
  name: string;
  icon: LucideIcon;
  status: "active" | "inactive";
  url: string;
}

export function QuickApps({ apps }: { apps: App[] }) {
  return (
    <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
      {apps.map((app) => (
        <Card
          key={app.id}
          className="group hover:scale-105 hover:shadow-xl hover:shadow-primary/10 transition-all cursor-pointer"
        >
          {/* 实现细节参考上文 */}
        </Card>
      ))}
    </div>
  );
}
```

### 示例 2: 创建订单列表页

```tsx
// app/(pages)/orders/page.tsx
import { Suspense } from "react";
import { OrdersTable } from "./components/orders-table";
import { OrderFilters } from "./components/order-filters";

export default async function OrdersPage() {
  return (
    <div className="p-6">
      <div className="mb-6">
        <h1 className="text-3xl font-bold">订单管理</h1>
        <p className="text-muted-foreground mt-2">管理销售订单</p>
      </div>

      <OrderFilters />

      <Suspense fallback={<TableSkeleton />}>
        <OrdersTable />
      </Suspense>
    </div>
  );
}
```

---

**文档版本**: v1.0
**最后更新**: 2026-02-13
**状态**: ✅ 已审核
